title = "Gitleaks config for Python library projects"

[allowlist]
description = "Allow common generated and local-only paths"
paths = [
    '''uv\.lock''',
    '''\.venv/''',
    '''\.pytest_cache/''',
    '''\.ruff_cache/''',
]
