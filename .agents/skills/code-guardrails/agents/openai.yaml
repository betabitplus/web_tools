interface:
  display_name: "Code Guardrails"
  short_description: "Avoid overcomplication and vague execution"
  default_prompt: "Use $code-guardrails to keep changes simple, surgical, assumption-aware, and tied to verifiable success criteria."
policy:
  allow_implicit_invocation: false
